import { ArrowRight, MapPin } from 'lucide-react'; import { Link } from 'react-router-dom'; import type { Lead } from '../../domain/leads/types'; import { Badge } from '../ui/Badge';
export function LeadRow({ lead }: { lead: Lead }) { return <Link to={`/leads/${lead.id}`} className="group flex min-w-0 items-center gap-3 border-b border-line/60 px-4 py-4 last:border-0 hover:bg-white/[.025] sm:gap-4 sm:px-5"><div className="grid h-9 w-9 shrink-0 place-items-center rounded-xl border border-line bg-navy text-xs font-semibold text-ice sm:h-10 sm:w-10 sm:text-sm">{lead.company.slice(0, 2).toUpperCase()}</div><div className="min-w-0 flex-1"><div className="truncate text-sm font-medium">{lead.company}</div><div className="mt-1 flex min-w-0 items-center gap-2 text-xs text-muted"><span className="truncate">{lead.segment}</span><span>·</span><span className="flex shrink-0 items-center gap-1"><MapPin size={11} />{lead.city}</span></div></div><div className="hidden text-right sm:block"><div className="font-mono text-sm text-ice">{lead.score}</div><div className="text-[10px] uppercase text-muted">score</div></div><Badge tone={lead.priority === 'Alta' ? 'gold' : lead.priority === 'Média' ? 'ice' : 'muted'}><span className="max-w-[7rem] truncate sm:max-w-none">{lead.stage}</span></Badge><ArrowRight size={16} className="shrink-0 text-muted transition group-hover:translate-x-1 group-hover:text-ice" /></Link>; }
