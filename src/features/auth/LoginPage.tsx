import { useState } from 'react'; import { Navigate } from 'react-router-dom'; import { supabase } from '../../lib/supabase'; import { BrandLogo } from '../../components/shared/BrandLogo'; import { useAuth } from './AuthProvider';
export function LoginPage(){const {user,loading}=useAuth();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [submitting,setSubmitting]=useState(false);if(loading)return <div className="grid min-h-screen place-items-center bg-navy text-sm text-muted">Carregando sessão…</div>;if(user)return <Navigate to="/" replace/>;const submit=async(e:React.FormEvent)=>{e.preventDefault();setSubmitting(true);setError('');const {error}=await supabase.auth.signInWithPassword({email,password});if(error)setError('Email ou senha inválidos.');setSubmitting(false)};return <main className="grid min-h-screen place-items-center bg-navy p-4"><section className="w-full max-w-md rounded-2xl border border-line bg-panel/80 p-6 shadow-2xl sm:p-8"><BrandLogo orientation="horizontal" theme="light" size="lg"/><div className="mt-8"><div className="font-mono text-[10px] uppercase tracking-[.24em] text-ice">Lead Intelligence</div><h1 className="mt-2 text-2xl font-semibold">Entrar no workspace</h1><p className="mt-2 text-sm leading-6 text-muted">Acesse a inteligência comercial da RanTech.</p></div><form onSubmit={submit} className="mt-7 space-y-4"><label className="block"><span className="mb-1.5 block text-xs text-muted">Email</span><input type="email" autoComplete="email" required value={email} onChange={e=>setEmail(e.target.value)} className="w-full rounded-lg border border-line bg-navy px-3 py-2.5 text-sm outline-none focus:border-ice" /></label><label className="block"><span className="mb-1.5 block text-xs text-muted">Senha</span><input type="password" autoComplete="current-password" required value={password} onChange={e=>setPassword(e.target.value)} className="w-full rounded-lg border border-line bg-navy px-3 py-2.5 text-sm outline-none focus:border-ice" /></label>{error&&<p role="alert" className="text-sm text-rose-200">{error}</p>}<button disabled={submitting} className="w-full rounded-xl bg-ice px-4 py-2.5 text-sm font-semibold text-navy disabled:opacity-60">{submitting?'Entrando…':'Entrar'}</button></form></section></main>}
