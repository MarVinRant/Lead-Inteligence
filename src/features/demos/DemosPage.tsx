import { ArrowRight } from 'lucide-react'; import { Link } from 'react-router-dom'; import { useLeads } from '../../hooks/useLeads'; import { Card } from '../../components/ui/Card'; import { PageHeader } from '../../components/shared/PageHeader';
export function DemosPage(){const {data=[]}=useLeads();const demos=[...new Map(data.map(l=>[l.recommendedDemo,l])).values()];return <><PageHeader eyebrow="Demo studio" title="Demos" description="Exemplos visuais associados às oportunidades reais do radar."/><div className="grid gap-4 sm:gap-5 md:grid-cols-2 lg:grid-cols-3">{demos.map((lead,i)=><Card key={lead.recommendedDemo} className="overflow-hidden"><div className={`h-36 p-5 sm:h-40 ${i===1?'bg-gradient-to-br from-[#42251e] to-[#0d2035]':'bg-gradient-to-br from-[#124760] to-[#0d2035]'}`}><div className="font-mono text-[10px] uppercase tracking-widest text-white/60">RanTech preview</div><div className="mt-10 text-xl font-semibold sm:mt-12">{lead.recommendedDemo}</div></div><div className="p-5"><div className="text-sm font-medium">{lead.recommendedService}</div><p className="mt-2 text-xs leading-5 text-muted">Associada a {lead.company}; pronta para personalização futura.</p><Link to={`/leads/${lead.id}`} className="mt-5 flex items-center gap-2 text-sm font-semibold text-ice">Ver lead associado <ArrowRight size={15}/></Link></div></Card>)}</div></>}
