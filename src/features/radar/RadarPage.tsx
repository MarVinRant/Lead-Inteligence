import { ChevronRight } from 'lucide-react'; import { Link } from 'react-router-dom'; import { useLeads } from '../../hooks/useLeads'; import { Card } from '../../components/ui/Card'; import { Badge } from '../../components/ui/Badge'; import { PageHeader } from '../../components/shared/PageHeader';
export function RadarPage(){const {data=[]}=useLeads();return <><PageHeader eyebrow="Opportunity radar" title="Radar" description="Oportunidades priorizadas por combinação de presença digital, intenção e fit de serviço."/><div className="grid gap-4 sm:gap-5 md:grid-cols-2">{[...data].sort((a,b)=>b.score-a.score).map((l,i)=><Card key={l.id} className="relative overflow-hidden p-4 sm:p-5"><div className={`absolute left-0 top-0 h-full w-1 ${i<2?'bg-gold':'bg-ice/40'}`}/><div className="flex items-start justify-between gap-3"><div className="min-w-0"><Badge tone={i<2?'gold':'ice'}>{i<2?'SINAL FORTE':'OPORTUNIDADE'}</Badge><h2 className="mt-4 truncate text-lg font-semibold">{l.company}</h2><div className="mt-1 truncate text-xs text-muted">{l.segment} · {l.city}</div></div><div className="shrink-0 text-right"><div className="font-mono text-2xl text-ice">{l.score}</div><div className="text-[10px] text-muted">SCORE</div></div></div><p className="mt-5 text-sm leading-6 text-muted">{l.diagnosis}</p><div className="mt-5 flex flex-wrap gap-2">{l.opportunities.map(o=><Badge key={o} tone="muted">{o}</Badge>)}</div><Link to={`/leads/${l.id}`} className="mt-5 flex items-center gap-2 text-sm font-semibold text-ice">Ver inteligência <ChevronRight size={15}/></Link></Card>)}</div></>}
